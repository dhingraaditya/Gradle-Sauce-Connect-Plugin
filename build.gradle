buildscript {
  repositories {
    maven {
      url "https://plugins.gradle.org/m2/"
    }
  }
  dependencies {
    classpath "com.gradle.publish:plugin-publish-plugin:latest.release"
  }
}

apply plugin: "com.gradle.plugin-publish"
apply plugin: "groovy"

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}

version = "0.0.15"
group = "SCPlugin"

pluginBundle {
  website = "https://github.com/dhingraaditya/Gradle-Sauce-Connect-Plugin"
  vcsUrl = "https://github.com/dhingraaditya/Gradle-Sauce-Connect-Plugin.git"
  description = "Un-Official release of SauceConnectPlugin"
  tags = ['SauceLabs', 'SauceConnect']

  plugins {
    SauceConnectPlugin {
      id = "com.aditya.SauceConnectPlugin"
      displayName = "Sauce Connect Plugin"
    }
  }
}
